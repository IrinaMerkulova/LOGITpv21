CREATE TABLE Autorid (
Autori_kood int primary key AUTO_INCRIMENT(1,1),
Nimi varchar(30),
Perenimi varchar(30),
Sünniaasta date,
Sugu varchar(30));

CREATE TABLE Keel( 
Keel_kood int primary key AUTO_INCREMENT,
Keel varchar (25) UNIQUE
);


CREATE TABLE Raamatud( 
Raamatu_kood int primary key identity(1,1),
Nimetus varchar (25) not null,
Autor int,
V_aasta int,
Keel int,
Hind decimal(5,2),
foreign key (Keel) references Keel(Keel_kood),
foreign key (Autor) references Autorid(Autori_kood)

CREATE TABLE Ostukorv( 
Ostu_ID int primary key AUTO_INCREMENT,
RaamatID int,
kuupaev date, 
Kogus int,
LinnID int,
foreign key (LinnID) references liin(linn_ID),
foreign key (RaamatID) REFERENCES raamatud(Raamatu_kood)
);

CREATE TABLE liin( 
linn_id int primary key AUTO_INCREMENT,
linnanimi varchar(20)
);

use AutoridTeran;
CREATE TABLE Laenutus( 
Laenutuse_kood int primary key AUTO_INCREMENT,
Klient int,
Raamat int, 
Algus_kuupaev date,
Lopp_kuupaev date,
foreign key (Klient) references Klient(Kliendi_kood),
foreign key (Raamat) references Raamatud(Raamatu_kood)
);

CREATE TABLE Laenutus( 
Laenutuse_kood int primary key iAUTO_INCREMENT,
Klient int,
Raamat int, 
Algus_kuupaev date,
Lopp_kuupaev date,
foreign key (Klient) references Klient(Kliendi_kood),
foreign key (Raamat) references Raamatud(Raamatu_kood)
);
